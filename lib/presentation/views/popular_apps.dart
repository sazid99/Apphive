import 'package:flutter/material.dart';

class PopularApps extends StatelessWidget {
  const PopularApps({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar());
  }
}
